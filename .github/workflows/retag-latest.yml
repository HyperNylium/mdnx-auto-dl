name: Retag to Latest

on:
  workflow_dispatch:
    inputs:
      old_tag:
        description: 'The existing tag to promote (e.g. v1.2.3)'
        required: true

jobs:
  retag:
    runs-on: ubuntu-latest
    steps:
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull the old image
        run: |
          docker pull ghcr.io/hypernylium/mdnx-auto-dl:${{ github.event.inputs.old_tag }}

      - name: Tag it as latest
        run: |
          docker tag \
            ghcr.io/hypernylium/mdnx-auto-dl:${{ github.event.inputs.old_tag }} \
            ghcr.io/hypernylium/mdnx-auto-dl:latest

      - name: Push the new latest tag
        run: |
          docker push ghcr.io/hypernylium/mdnx-auto-dl:latest
